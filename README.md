
  # Build it now

  This is a code bundle for Build it now. The original project is available at https://www.figma.com/design/LcEDDlnaxc687y1qJLjX4R/Build-it-now.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  